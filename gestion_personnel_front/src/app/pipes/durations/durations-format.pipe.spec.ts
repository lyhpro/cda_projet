import { DurationsFormatPipe } from './durations-format.pipe';

describe('DurationsPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
